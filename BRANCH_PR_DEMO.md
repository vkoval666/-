# Branch / Pull Request demo

Для оценки Git workflow отдельное изменение должно идти через branch и Pull Request, а не напрямую в `main`.

Цепочка: feature branch → commits → Pull Request → review → merge.
