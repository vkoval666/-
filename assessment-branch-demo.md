# Assessment branch demo

Это изменение сделано в отдельной ветке `assessment/real-project-context` для демонстрации Git workflow.

После проверки ветка должна быть объединена с `main` через Pull Request.
